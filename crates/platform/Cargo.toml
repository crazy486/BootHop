[package]
name = "boothop-platform"
version = "0.1.0"
edition = "2024"

[dependencies]
boothop-core = { path = "../core" }

[target.'cfg(target_os = "linux")'.dependencies]
rustix = { version = "=1.1.4", features = ["fs"] }
