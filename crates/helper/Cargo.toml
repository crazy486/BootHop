[package]
name = "boothop-helper"
version = "0.1.0"
edition = "2024"

[dependencies]
boothop-core = { path = "../core" }
boothop-platform = { path = "../platform" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
libc = "0.2"
