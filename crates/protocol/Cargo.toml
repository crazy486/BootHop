[package]
name = "boothop-protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
boothop-core = { path = "../core" }
getrandom = "0.3.4"
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = { version = "=1.0.151", features = ["raw_value"] }
